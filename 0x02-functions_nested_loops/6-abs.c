#include "main.h"
/**
 * _abs - Computes the absolute value
 * of an integer.
 *
 * @i: Inputs number as an integer.
 *
 * Return: Absolute value
 */
int _abs(int i)
{
	if (n >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
